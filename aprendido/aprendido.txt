Este api es un sistema de gestion de usuario,usaremos servidores en la nube para guardarlos

En este proyecto primero comentare acerca de la paginacion y iniciacion  de la tabla:

(Diseño de la base da datos cuanto te da error de phone no puede ser null.cambiar en mysql la pestaña Estructura y poner null ✓)

Debo buscar en el html llamado usuarios "originalmente era datatables-demo.js" la linea de la tabla y buscar el id"DataTable"

Debo buscar con shift x2 en mi IDE,en todo el proyecto la funcion en javascript que carga la tabla,el id de la tabla se buscar
#DataTable
y agregar una funcion para cargar los usuarios de la base de datos,debo crear la funcion de cargarUsuarios"fetch example se llama la funcion para hacer la request HTTP".

Debo poner la funcion cargarUsuarios dentro de la funcion asincrona que carga la tabla que es la que tiene el id



Cada funcion tendra su request y cambiara sus metodos”DELETE,POST,GET”

La funcion deleteUser solo cambiara el GET por el DELETE,contara con la funcion confir(),ademas de racargar la pagina una vez eliminado un usuario location.reload()
-------------------------------------------------------------------
*El archivo register.js tendra la funcion registrarUsuario() y el metodo POST para ingresar los usuarios y agregarlos a la BD 
la clase UserController debe crearse el metodo para registrar el usuario en la BD,este metodo tendra otro metodo de la capa 
DAO register(),entityManeger.marge(user)

Anotacion:Cuando se haga click en el boton Registrar se tendra que capturar los datos del usuario y enviarlo al server.

*En register.js Necesitamos enviar datos del usuarios(nombre,apellido,email).Necesitamos extraer el input del html que es
donde el usuario escribio los datos ej,nombre,apellido,email.

Por lo tanto buscamos esa caja de texto donde estan estos datos.id = "exampleFirstName"
Luego en el navegador en console busco un elemento por su atributo id:document.getElementById(`txtName`).value
Esto lo que hace es que cuando nosotros completemos la casilla nombre me devuelva el nombre que puso el usuario.

Quiero que cuando se aprete el boton registrar se ejecute la funcion que guarda los datos en la BD.la funcion se crea en el 
register.js y se coloca(llama) en el register.html => boton Registrarse/register agregar onclick="registrar()"

----------------
¿Donde se llaman las funciones de javascript?
En la funcion login.js se llamara en login.html y se usara cuando el usuario oprima el boton registrar
---------------
URL QUE USARA LA API REST
Los metodos POST,DELETE,GET usaran la misma URL "endpoint" que hay en el Controller y la funcion loadUser() de javascript.
DELETE usar `api/users/{id}`.No devuelve nada

(REST)
La URL identifica el recurso
El método HTTP define la acción

En UserController,en los metodos HTTP: DELETE puede usar un console.log() para ver lo que devuelve este metodo,Se agrega
return "" en el metodo de UserController que toma el DELETE"deleteUser" de javascript, se codifica console.log(message)

:::::::A TENER EN CUENTA:::::::::
  JS → HTTP → Spring → método Java
  Spring solo enruta según:
  📍 URL
  📍 método HTTP


