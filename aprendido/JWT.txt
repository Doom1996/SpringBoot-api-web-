JWTUtil se encarga de:

✅ Crear tokens JWT cuando un usuario se logea
✅ Validar tokens cuando el usuario hace requests
✅ Extraer datos del token (id, email/usuario)

ees decir:convierte datos de usuario → en un token seguro → que viaja en cada request

La firma se hace con mi key secreta.

Si alguien lo modifica → la firma ya no coincide → se invalida.

*El token NO es encriptado.Es firmado.Cualquiera puede leerlo, pero;nadie puede modificarlo sin tu key.

En resumen:
✔ El backend controla la sesión con tiempo
✔ El token se invalida solo
✔ No usás sesiones de servidor (escalable)

Esto es arquitectura moderna de APIs REST

Ademas debemos configurar application.properties(Es el archivo de configuración central de Spring Boot)

✅ application.properties define comportamiento del sistema
✅ Evita valores hardcodeados
✅ Permite seguridad y flexibilidad
✅ Spring lo inyecta automáticamente en clases

En una frase profesional:

application.properties desacopla la configuración de la lógica de negocio.

----------------
La funcion que cumple :weses Representar al usuario autenticado mediante un token firmado.

JWT contiene:

• id de usuario
• email/roles
• expiración
• firma segura

JWT (Token): Se usa para la seguridad en tránsito. Es la credencial
que el servidor le entrega al cliente después de que este demostró
quién es. Permite que el usuario navegue por la app sin tener que 
enviar su contraseña en cada clic.

JWT y Argon2 crean Un mecanismo eficiente y escalable para manejar sesiones en el frontend.
Argon2 protege las contraseñas en reposo, y JWT gestiona la autenticación del usuario una vez verificada su identidad.
-------------------------------------------------------:weses
💬 “¿Cómo manejás autenticación segura en tu API?”

Podés decir:

Las contraseñas se hashean con Argon2 antes de persistirse en base de datos.
Durante el login se verifica el hash, y si es válid o se genera un JWT firmado
que el cliente utiliza para autenticarse en las solicitudes posteriores 
evitando el uso de sesiones de servidor.


                  ::::RESUMEN::::Argon2 asegura las credenciales; JWT asegura la sesión.

| Seguridad | Sirve para          | Cuándo se usa     |
| --------- | ------------------- | ----------------- |
| Argon2    | proteger passwords  | registro + login  |
| JWT       | autenticar requests | después del login |

---------------(JWT necesita una clave privada del backend)

La clave definida en security.jwt.secret(application.properties) se utiliza exclusivamente para la firma y
validación de los tokens JWT, mientras que Argon2 opera como algoritmo de hashing 
de contraseñas sin necesidad de secretos compartidos del servidor.





